{
  "devDependencies": {
    "karma": "~0.10",
    "karma-requirejs": "~0.2",
    "karma-firefox-launcher": "~0.1"
  },
  "scripts": {
     "test": "karma start --single-run --browsers Firefox config/karma.conf"
  }
}
